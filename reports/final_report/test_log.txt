Acceptance test log
Date/time: 2026-06-06, Asia/Bangkok

Project inspected:
- temperature_forecast
- Topic: Predict next-day temperature using LSTM with TensorFlow/Keras

Commands checked:
- python main.py self_test: succeeded.
- python app_temperature_cli.py self_test: succeeded.
- python tests/test_temperature_end_to_end.py: succeeded.
- pip install -r requirements.txt: attempted, but did not complete in the current MSYS2 Python environment.

Expected outputs:
- self_test prints required folders, files, module import status, configured raw data path, configured model path, and clear missing-artifact notices.
- preprocess should read data/raw/temperature.csv and create processed CSV/JSON files.
- train should call the real LSTM training implementation once src/train_lstm.py is completed.
- evaluate should require processed data and a trained model before computing MAE, MSE, RMSE.
- predict_next_day should require processed data and a trained model before writing a prediction.

Existing artifacts found during inspection:
- data/raw/temperature.csv
- data/processed/temperature_clean.csv
- data/processed/temperature_train_scaled.csv
- data/processed/temperature_val_scaled.csv
- data/processed/temperature_test_scaled.csv
- data/processed/temperature_scaled.csv
- data/processed/split_info.json
- models/scaler_params.json
- reports/01_data_profile/raw_data_profile.json
- reports/01_data_profile/clean_data_profile.json

Missing artifacts:
- models/temp_lstm.keras
- training history output from a real training run
- evaluation metrics output from a real evaluation run
- prediction output from a real prediction run
- training/evaluation figures from real runs

Dependency/environment notes:
- The active Python environment did not have pandas installed when self_test was run, so imports for src.data_utils and src.preprocess_timeseries reported "No module named 'pandas'".
- The first pip install attempt was blocked by network permissions.
- The second pip install attempt was allowed to access the network, but failed because the current MSYS2 Python environment is externally managed and a build dependency failed with SSL certificate verification errors.
- Recommended next step: create a virtual environment with python -m venv .venv, activate it, then rerun pip install -r requirements.txt.

Placeholder or incomplete modules:
- src/train_lstm.py currently appears to be a placeholder.
- src/evaluate_lstm.py currently appears to be a placeholder.
- src/predict_next_day.py currently appears to be a placeholder.
- src/model_lstm.py currently appears to be a placeholder.
- src/windowing.py currently appears to be a placeholder.

Notes for the group before submission:
- Do not report MAE, MSE, RMSE, loss, figures, or predictions until the real train/evaluate/predict modules are implemented and run.
- Keep chronological train/validation/test splitting. Do not shuffle the time series.
- Fit normalization parameters on the train split only.
- After the model code is completed, rerun train, evaluate, predict_next_day, then update this log with the real command results.

Cleanup run:
- Removed Python cache folders/files: __pycache__/ and *.pyc.
- Removed requirements-data.txt because it duplicated part of requirements.txt and was not referenced.
- Kept processed data, scaler parameters, and data profile JSON files because they are existing preprocessing artifacts.
- Kept placeholder LSTM modules for group confirmation because they match the expected project pipeline.

Validation after cleanup:
- python main.py self_test: succeeded. The command still reports missing pandas in the current environment and missing models/temp_lstm.keras, but exits successfully with readable notices.
- python app_temperature_cli.py self_test: succeeded with the same readable notices.
- python tests/test_temperature_end_to_end.py: succeeded.
- Python cache folders generated by validation were removed again before finalizing cleanup.
